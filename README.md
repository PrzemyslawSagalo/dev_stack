# DEV STACK
## Architectural drivers
### This repo
* This is a standalone repository, allowing to change, test, and push.
* Bash scripts are used because they can be run anywhere without requiring much configuration, whereas Ansible can be more challenging to configure.
### raindrop
* Even if a page is down I should have an access.
